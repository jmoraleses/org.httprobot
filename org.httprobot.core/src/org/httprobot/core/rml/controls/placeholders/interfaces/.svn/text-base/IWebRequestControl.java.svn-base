/**
 * 
 */
package org.httprobot.core.rml.controls.placeholders.interfaces;

import org.httprobot.common.events.RmlEventArgs;
import org.httprobot.common.exceptions.InconsistenMessageException;
import org.httprobot.common.exceptions.NotImplementedException;
import org.httprobot.common.interfaces.IRmlListener;

/**
 * @author Joan
 *
 */
public interface IWebRequestControl extends IRmlListener
{
	/**
	 * Fired when WebRequest must be read. Control loaded.
	 * @param sender {@link Object} the sender
	 * @param e {@link RmlEventArgs} the arguments
	 * @throws NotImplementedException when the implementation is already undone	 
	 * @throws InconsistenMessageException when message data doesn't match with corresponding controller.
	 */
	public void OnWebRequestInit(Object sender, RmlEventArgs e) throws NotImplementedException;
	/**
	 * Fired when WebRequest must be read. Control loaded.
	 * @param sender {@link Object} the sender
	 * @param e {@link RmlEventArgs} the arguments
	 * @throws NotImplementedException when the implementation is already undone	 
	 * @throws InconsistenMessageException when message data doesn't match with corresponding controller.
	 */
	public void OnWebRequestRead(Object sender, RmlEventArgs e) throws NotImplementedException;
	/**
	 * Fired when WebRequest must be read. Control loaded.
	 * @param sender {@link Object} the sender
	 * @param e {@link RmlEventArgs} the arguments
	 * @throws NotImplementedException when the implementation is already undone	 
	 * @throws InconsistenMessageException when message data doesn't match with corresponding controller.
	 */
	public void OnWebRequestLoaded(Object sender, RmlEventArgs e) throws NotImplementedException;
	/**
	 * Fired when WebRequest must be read. Control loaded.
	 * @param sender {@link Object} the sender
	 * @param e {@link RmlEventArgs} the arguments
	 * @throws NotImplementedException when the implementation is already undone	 
	 * @throws InconsistenMessageException when message data doesn't match with corresponding controller.
	 */
	public void OnWebRequestChanged(Object sender, RmlEventArgs e) throws NotImplementedException;
	/**
	 * Fired when WebRequest must be read. Control loaded.
	 * @param sender {@link Object} the sender
	 * @param e {@link RmlEventArgs} the arguments
	 * @throws NotImplementedException when the implementation is already undone	 
	 * @throws InconsistenMessageException when message data doesn't match with corresponding controller.
	 */
	public void OnWebRequestRendered(Object sender, RmlEventArgs e) throws NotImplementedException;
	/**
	 * Fired when WebRequest must be read. Control loaded.
	 * @param sender {@link Object} the sender
	 * @param e {@link RmlEventArgs} the arguments
	 * @throws NotImplementedException when the implementation is already undone	 
	 * @throws InconsistenMessageException when message data doesn't match with corresponding controller.
	 */
	public void OnWebRequestWrite(Object sender, RmlEventArgs e) throws NotImplementedException;
}